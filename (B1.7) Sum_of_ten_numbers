def main():
    summation = 0    # Initialize sum

    for i in range(10):
      num = int(input(f"Enter number {i + 1}: "))    # Get user input
      summation += num   # Add number to total

    print("The sum of all numbers is:", summation)   # Print sum

main()
