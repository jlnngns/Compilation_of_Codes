# Ask the user to input two numbers
dividend = int(input("Enter the first number: "))
divisor = int(input("Enter the second number: "))

# Check if the divisor is not zero 
if divisor == 0:
   print("Error! Division ny zero is not allowed.")
else:
   # Perform division and remove decimal part
   quotient = dividend // divisor    # Floor division removes the decimal part
   print("The quotient without the decimal point is:", quotient)
