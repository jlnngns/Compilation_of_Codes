# Initialize the number
value = 0

# Use a while loop to iterate from 0 to 100
while value <= 100:
    # Check if the number does not end in 0 or 5
    if value % 10 != 0 and value % 10 != 5:
          print(value)
    value += 1    # Increment the number
